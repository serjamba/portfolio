export function initGallery(){const links=[...document.querySelectorAll('[data-gallery]')],dialog=document.querySelector('.gallery-dialog'),img=dialog.querySelector('img'),error=dialog.querySelector('.gallery-error');let current=0,token=0,origin;
 async function show(index){current=(index+links.length)%links.length;const a=links[current],t=++token;dialog.querySelector('.gallery-count').textContent=(current+1)+' / '+links.length;dialog.querySelector('#gallery-caption').textContent=a.querySelector('img').alt;img.hidden=true;error.hidden=true;const next=new Image();next.src=a.href;next.alt=a.querySelector('img').alt;try{await next.decode();if(t!==token)return;img.src=next.src;img.alt=next.alt;img.hidden=false;}catch{if(t===token)error.hidden=false;}}
 links.forEach((a,i)=>a.addEventListener('click',e=>{e.preventDefault();origin=a;dialog.showModal();document.body.classList.add('locked');show(i);}));
 dialog.querySelector('.gallery-close').addEventListener('click',()=>dialog.close());dialog.querySelector('.gallery-prev').addEventListener('click',()=>show(current-1));dialog.querySelector('.gallery-next').addEventListener('click',()=>show(current+1));dialog.addEventListener('keydown',e=>{if(e.key==='ArrowRight'){e.preventDefault();show(current+1);}if(e.key==='ArrowLeft'){e.preventDefault();show(current-1);}});dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close();}});dialog.addEventListener('close',()=>{token++;document.body.classList.remove('locked');origin?.focus({preventScroll:true});});
}

