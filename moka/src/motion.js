const system=matchMedia('(prefers-reduced-motion:reduce)');let preference='on';try{preference=localStorage.getItem('moka:motion:v1')||'on';}catch{}
export const motion={enabled:false};
export function initMotion(){const buttons=[...document.querySelectorAll('.motion-toggle')];function sync(){motion.enabled=preference!=='off'&&!system.matches;document.body.classList.toggle('motion-on',motion.enabled);document.body.classList.toggle('motion-off',!motion.enabled);buttons.forEach(b=>{b.hidden=false;b.textContent=system.matches?'Без движения':motion.enabled?'Пауза движения':'Включить движение';b.disabled=system.matches;b.setAttribute('aria-pressed',String(!motion.enabled));});dispatchEvent(new CustomEvent('moka:motion',{detail:motion.enabled}));}buttons.forEach(b=>b.addEventListener('click',()=>{preference=motion.enabled?'off':'on';try{localStorage.setItem('moka:motion:v1',preference);}catch{}sync();}));system.addEventListener('change',sync);sync();
 const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){if(motion.enabled)e.target.classList.add('section-enter');io.unobserve(e.target);}}),{threshold:.12});document.querySelectorAll('#about,#coffee,#atmosphere').forEach(el=>io.observe(el));
}
