TQ.searchProducts=query=>{const q=query.trim().toLocaleLowerCase('ru');if(!q)return [];return TQ.products.filter(p=>[p.name,p.brand,p.sku,p.oem,p.params].join(' ').toLocaleLowerCase('ru').includes(q));};
TQ.searchPage=()=>{const mode=TQ.current.params.get('mode')||'oem',q=TQ.current.params.get('q')||'';return `<div class="container page">${TQ.crumbs([['Поиск запчастей']])}${TQ.heading('Найдём нужную деталь','По артикулу, названию или вашему автомобилю.')}<div class="search-layout"><section><div class="tabs"><a class="tab ${mode==='oem'?'active':''}" href="#/search?mode=oem">OEM / артикул</a><a class="tab ${mode==='vin'?'active':''}" href="#/search?mode=vin">VIN автомобиля</a><a class="tab ${mode==='name'?'active':''}" href="#/search?mode=name">Название детали</a></div>${mode==='vin'?`<form class="panel search-form" data-form="vin"><label class="field">VIN автомобиля<div class="search-large"><input id="vin-input" name="vin" maxlength="17" placeholder="Введите 17 символов VIN" autocomplete="off" autocapitalize="characters" aria-describedby="vin-help vin-error"><button class="btn">Найти автомобиль</button></div></label><div class="row between" style="margin-top:12px"><p id="vin-help" class="small">Латинские буквы и цифры, без I, O, Q</p><span id="vin-length" class="small muted">0 / 17</span></div><p id="vin-error" class="error" hidden></p><details><summary>Где найти VIN?</summary><p>В свидетельстве о регистрации, под лобовым стеклом или на стойке двери. </p></details><div class="search-examples"><span>Примеры:</span><button type="button" class="text-btn" data-action="vin-example" data-value="${TQ.demoVIN}">BMW G20</button><button type="button" class="text-btn" data-action="vin-example" data-value="${TQ.clarifyVIN}">Уточнение Audi</button></div></form>`:`<form class="panel search-form" data-form="search"><label class="field">${mode==='name'?'Название детали':'Артикул или OEM'}<div class="search-large"><input name="q" type="search" placeholder="${mode==='name'?'Например, амортизатор':'Например, TQ-DEMO-001'}" value="${TQ.esc(q)}" required><button class="btn">Найти ${TQ.icon('search','white')}</button></div></label><div class="search-examples"><span>Например:</span><a href="#/search?q=TQ-DEMO-001">TQ-DEMO-001</a><a href="#/search?q=DEMO-OEM-330">DEMO-OEM-330</a><a href="${TQ.route('search',{mode:'name',q:'амортизатор'})}">Амортизатор</a></div></form>`}<div id="search-result" class="search-result">${mode!=='vin'&&q?TQ.searchResults(q):`<div class="search-intro"><div class="eyebrow muted">Точность начинается с подбора</div><h2>${mode==='vin'?'Сначала автомобиль. Затем детали.':'У каждой детали есть свой номер.'}</h2><p>${mode==='vin'?'Введите VIN, чтобы определить автомобиль и уточнить модификацию.':'Артикул можно найти на упаковке или корпусе детали. Поиск также работает по названию и производителю.'}</p></div>`}</div></section><aside class="search-aside panel">${TQ.icon('shield-check')}<h3>Совпадение артикула — только первый шаг</h3><p>После поиска проверьте применимость к вашей модификации автомобиля.</p><button class="btn secondary full" data-action="car-modal">Выбрать автомобиль</button><div class="rule"></div><p class="small">Не нашли нужный номер? Уточните модификацию или обратитесь за помощью.</p></aside></div></div>`;};
TQ.searchResults=q=>{const list=TQ.searchProducts(q);const exact=list.filter(p=>p.sku.toLowerCase()===q.toLowerCase()||p.oem.toLowerCase()===q.toLowerCase());if(!list.length)return TQ.empty('Ничего не найдено',`По запросу «${TQ.esc(q)}» нет совпадений в каталоге. Проверьте номер или выберите автомобиль.`,'Подбор по VIN','#/search?mode=vin','search');return `<div class="section-head"><h2>${exact.length?'Точное совпадение':'Результаты поиска'}</h2><span class="small muted">${list.length} ${list.length===1?'товар':'товаров'}</span></div>${exact.length>1?'<div class="notice" style="margin-bottom:20px">Этот номер встречается у нескольких производителей. Выберите бренд и проверьте совместимость.</div>':''}${TQ.productGrid(list)}${exact.length===1?`<section class="section"><div class="section-head"><h2>Аналоги</h2></div>${TQ.productGrid(TQ.products.filter(p=>p.category===exact[0].category&&p.id!==exact[0].id).slice(0,3))}</section>`:''}<div class="notice neutral" style="margin-top:24px">Нужен другой раздел? <a class="link" href="#/catalog">Открыть категории →</a></div>`;};
TQ.submitVIN=form=>{const value=form.elements.vin.value.trim().toUpperCase();const error=document.getElementById('vin-error');error.hidden=true;if(!/^[A-HJ-NPR-Z0-9]{17}$/.test(value)){error.textContent='VIN должен содержать ровно 17 латинских букв и цифр. Буквы I, O и Q не используются.';error.hidden=false;form.elements.vin.setAttribute('aria-invalid','true');return;}form.elements.vin.removeAttribute('aria-invalid');const result=document.getElementById('search-result');result.innerHTML='<div class="loading" role="status">Ищем автомобиль…</div>';const stamp=TQ.renderStamp;clearTimeout(TQ.searchTimer);TQ.searchTimer=setTimeout(()=>{if(TQ.renderStamp!==stamp)return;const car=value===TQ.demoVIN?TQ.cars[0]:value===TQ.clarifyVIN?TQ.cars[1]:null;if(!car){result.innerHTML=TQ.empty('Формат верный. Данных нет.','Не удалось определить автомобиль. Проверьте номер или уточните данные у специалиста.','Запрос специалисту','#/contacts','car');return;}const clarify=car.id==='audi';result.innerHTML=`<div class="panel vin-result"><span class="chip">${clarify?'Требуется уточнить модификацию':'Автомобиль распознан'}</span><div class="row" style="margin:24px 0">${TQ.icon('car')}<div><h2>${car.make} ${car.model}</h2><p>${car.year} · ${clarify?'Выберите двигатель ниже':car.engine}</p></div></div>${clarify?`<label class="field" style="max-width:360px;margin-bottom:24px">Модификация<select id="vin-modification"><option value="">Уточните модификацию</option><option value="audi">2.0 TFSI</option></select></label>`:''}<div class="row"><button class="btn" data-action="vin-save" data-id="${car.id}" ${clarify?'disabled':''}>Добавить в гараж</button><button class="btn secondary" data-action="vin-catalog" data-id="${car.id}" ${clarify?'disabled':''}>Перейти к категориям</button></div></div>`;},0);};
